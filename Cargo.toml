[package]
name = "termphin-agent"
version = "0.6.0"
edition = "2024"
license = "MIT"
description = "Persistent single-PTY session helper for Termphin"

[dependencies]
libc = "0.2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true
